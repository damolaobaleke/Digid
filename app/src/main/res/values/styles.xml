<resources>

    <!-- Base application theme. MaterialComponents -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/mb_transparent</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Username">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/amiko</item>
    </style>

    <style name="Password">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/amiko</item>
    </style>

    <style name="identityInfo">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="cardInfo">
        <item name="android:textColor">@color/mb_black</item>
        <item name="android:fontFamily">@font/kredit_front</item>
        <item name="android:textSize">22sp</item>
    </style>

    <!--Bottom Sheet
    The BottomSheetDialog is setting a default white background color , this is why the corners are not visible,
    In order to show them you need to make the background of the dialog transparent-->
    <style name="BottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/bottomSheetStyle</item>
    </style>

    <style name="bottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/white</item>
        <item name="android:paddingBottom">30dp</item>
        <item name="android:paddingTop">20dp</item>
        <item name="android:radius">20dp</item>
    </style>
    <!--Bottom Sheet-->

    <!--Bottom Sheet V2-->
    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/customCurvedBottomSheet</item>
    </style>

    <style name="customCurvedBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_dialogue</item>
        <item name="android:paddingBottom">30dp</item>
        <item name="android:paddingTop">20dp</item>
    </style>

</resources>
